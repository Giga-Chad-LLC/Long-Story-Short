/**
 * Comes without a trailing slash, therefore the client should insert it manually when a route is requested.
 */
export const SERVER_API_URL = "http://127.0.0.1:8000"

/**
 * Come without leading slash, therefore the client should insert it manually when a route is requested.
 */
export const routes = {
  summarize: "summarize"
};
